/** Border radius from mockup: DEFAULT 4px, lg 8px, xl 12px, full pill. */
export const radius = {
  none: 0,
  default: 4,
  sm: 4,
  md: 8,
  lg: 12,
  xl: 12,
  '2xl': 16,
  full: 9999,
} as const
