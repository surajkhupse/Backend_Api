import { AuthPlaceholderPage } from '../auth'

export function ForgotPasswordPage() {
  return (
    <AuthPlaceholderPage
      title="Reset password"
      description="Password reset UI is coming soon. Use POST /api/auth/forgot-password from the API docs."
    />
  )
}
